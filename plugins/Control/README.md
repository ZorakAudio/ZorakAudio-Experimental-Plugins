# Control

Gestural, modulation, and control-surface style tools.

## Plugins

- `GesturePad` — GesturePad (JSFX)

Each plugin leaf carries its own `plugin.json`, source entry, and optional tests/docs/assets.
